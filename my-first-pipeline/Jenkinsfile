currentBuild.displayName = "newfile-#"+currenrtBuild.number
pipeline {
  agent any
  stages {
    stage("test") {
      steps {
        echo "i am creating a file"
      }
    }
     stage("mvn check") {
      steps {
        echo "this is checking with mvn"
        sh "mvn clean package"
      }
    }
  }
}
